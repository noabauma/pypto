# `pl.tensor`

张量级算子 —— 接受并返回 `pl.Tensor` 值，因此属于编排层，或任何以整张张量为工作单位的地方。见[选择命名空间](../user/ops/00-dispatch.md)。

::: pypto.language.tensor
    options:
      show_root_heading: false
      filters: ["!^_"]
