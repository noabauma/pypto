# `pl.optimizations`

`pl.at(..., optimizations=[...])` 列表接受的那些优化项 —— `pl.split`、`pl.cross_core_slot` 等。见[运行时开销](../user/performance/02-runtime-overhead.md)。

::: pypto.language.optimizations
    options:
      show_root_heading: false
      filters: ["!^_"]
