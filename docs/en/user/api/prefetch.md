# `pl.prefetch`

Asynchronous GM to L2 prefetch. The handle types these produce are described in the
[feature matrix](../appendix/00-feature-matrix.md).

::: pypto.language.prefetch
    options:
      show_root_heading: false
      members_order: source
      filters: ["!^_"]
