# `pl.optimizations`

The optimization entries a `pl.at(..., optimizations=[...])` list accepts — `pl.split`,
`pl.cross_core_slot` and friends. See [Runtime overhead](../user/performance/02-runtime-overhead.md).

::: pypto.language.optimizations
    options:
      show_root_heading: false
      filters: ["!^_"]
